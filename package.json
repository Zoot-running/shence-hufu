{
  "name": "@shence/hufu",
  "version": "0.1.0",
  "description": "虎符 —— 神策 P1：DSH 并行调度（槽位状态机/工作项账本/心跳/恢复）",
  "type": "module",
  "main": "lib/index.js",
  "types": "src/index.ts",
  "files": [
    "lib",
    "src",
    "cordis.patch.yml"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --packages=external --outfile=lib/index.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
